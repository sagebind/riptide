import 'builtins' for *

$GLOBALS->modules = [
    # A list of module loader functions.
    loaders: []

    # A map of module paths to their preloaded contents.
    loaded: [:]
]

$GLOBALS->pwd = sub pwd {
    return @cwd
}
