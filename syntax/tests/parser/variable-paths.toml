source = '''
$lang->ext->panic 'hello world'

println $mytable
println $mytable->'key with spaces'->bar
'''
ast = '''
Block(
    named_params: None,
    vararg_param: None,
    statements: [
        Pipeline(Pipeline([
            UnnamedCall(
                function: MemberAccess(MemberAccess(Variable("lang"), "ext"), "panic"),
                args: [
                    Expr("hello world"),
                ],
            ),
        ])),
        Pipeline(Pipeline([
            NamedCall(
                function: "println",
                args: [
                    Expr(Variable("mytable")),
                ],
            ),
        ])),
        Pipeline(Pipeline([
            NamedCall(
                function: "println",
                args: [
                    Expr(MemberAccess(MemberAccess(Variable("mytable"), "key with spaces"), "bar")),
                ],
            ),
        ])),
    ],
)
'''
