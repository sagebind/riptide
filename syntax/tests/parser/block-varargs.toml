source = '''
call <...args> {
    println
}
'''
ast = '''
Block(
    named_params: None,
    vararg_param: None,
    statements: [
        Pipeline(Pipeline([
            NamedCall(
                function: "call",
                args: [
                    Expr(Block(
                        named_params: None,
                        vararg_param: Some("args"),
                        statements: [
                            Pipeline(Pipeline([
                                NamedCall(
                                    function: "println",
                                    args: [],
                                ),
                            ])),
                        ],
                    )),
                ],
            ),
        ])),
    ],
)
'''
