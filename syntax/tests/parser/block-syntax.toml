source = '''
# One-liner
call { println }

# With newlines
call {
    println
}

# Scrunched up
call{println}
'''
ast = '''
Block(
    named_params: None,
    vararg_param: None,
    statements: [
        Pipeline(Pipeline([
            NamedCall(
                function: "call",
                args: [
                    Expr(Block(
                        named_params: None,
                        vararg_param: None,
                        statements: [
                            Pipeline(Pipeline([
                                NamedCall(
                                    function: "println",
                                    args: [],
                                ),
                            ])),
                        ],
                    )),
                ],
            ),
        ])),
        Pipeline(Pipeline([
            NamedCall(
                function: "call",
                args: [
                    Expr(Block(
                        named_params: None,
                        vararg_param: None,
                        statements: [
                            Pipeline(Pipeline([
                                NamedCall(
                                    function: "println",
                                    args: [],
                                ),
                            ])),
                        ],
                    )),
                ],
            ),
        ])),
        Pipeline(Pipeline([
            NamedCall(
                function: "call",
                args: [
                    Expr(Block(
                        named_params: None,
                        vararg_param: None,
                        statements: [
                            Pipeline(Pipeline([
                                NamedCall(
                                    function: "println",
                                    args: [],
                                ),
                            ])),
                        ],
                    )),
                ],
            ),
        ])),
    ],
)
'''
